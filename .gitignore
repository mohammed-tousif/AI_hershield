# Secrets — never commit
.env
*.local.env
hershield-firebase-adminsdk.json
*-firebase-adminsdk*.json

# Dependencies & runtime
node_modules/
npm-debug.log*

# Gender-verification selfies — privacy-sensitive, never commit (see routes/verification.js)
uploads/

# OS / editor
.DS_Store
Thumbs.db
